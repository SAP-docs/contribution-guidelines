export interface TranslationJson {
    collections: Collection[];
    defaultConfiguration?: Partial<Folder>;
}
export interface Collection {
    collectionName: string;
    folders: Folder[];
}
export interface Folder {
    /**
     * Source directory
     */
    startingFolderPath?: string;
    /**
     * Target directory
     */
    targetFolderPath?: string;
    /**
     * List of source inclusion filters, e.g. `["&#42;&#42;/*.html"]` or
     * `["fileOne.md", "path/to/fileTwo.md"]`.
     */
    sourceFilters?: string[];
    /**
     * Naming convention for files copied from source to target directory, e.g.
     * `"[langCode].[extension]"`
     */
    targetFileNamingConvention?: string;
}
//# sourceMappingURL=json.d.ts.map