import { Tokenizer } from ".";
export interface TokenizerOptions {
    nodeType: string;
    isIllegalChar?: (charCode: number) => boolean;
}
export interface FenceOptions {
    open?: string;
    close?: string;
}
export interface DefaultFenceOptions extends FenceOptions {
    open: string;
    close: string;
}
export declare function factory(defaultOptions: DefaultFenceOptions & TokenizerOptions): (opts?: FenceOptions) => Tokenizer;
//# sourceMappingURL=fenced-tokenizer.d.ts.map