import { Observable } from "rxjs";
import { MkdocsConfig } from ".";
export declare type VariableValue = string | undefined | object;
export interface Variable {
    name: string;
    value: VariableValue;
}
export interface ResolverResult {
    resolvedText: string;
    variables: Variable[];
}
export declare class VariableResolver {
    static VARIABLE_REGEX: RegExp;
    config: Promise<MkdocsConfig>;
    constructor(config$: Observable<MkdocsConfig>);
    resolveVariable(variableName: string): Promise<VariableValue>;
}
//# sourceMappingURL=resolver.d.ts.map